---
description: Displays a snapshot of token consumption for the current session (total, prompt ranking, breakdown by skill, % context window).
---

Run:

```bash
wick snapshot “$(pwd)”
```

Display the command's output to the user exactly as it appears (already formatted), without summarizing or reformatting it.
